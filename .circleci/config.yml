version: 2
jobs:
  build:
    docker:
      - image: circleci/java:11.0.3
    steps:
      - checkout
      - run: <command>
  test:
    docker:
      - image: circleci/java:11.0.3
    steps:
      - checkout
      - run: <command>
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
